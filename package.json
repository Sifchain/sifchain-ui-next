{
  "name": "sifchain",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "prepare": "husky install",
    "build": "turbo run build",
    "dev": "turbo run dev --scope=@sifchain/dex",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "ui:storybook": "pnpm -C ./packages/ui run storybook ",
    "docs:dev": "pnpm -C ./apps/docs dev"
  },
  "keywords": [],
  "license": "Apache-2.0",
  "devDependencies": {
    "@changesets/cli": "^2.22.0",
    "@commitlint/cli": "^17.0.0",
    "@commitlint/config-conventional": "^17.0.0",
    "husky": "^8.0.0",
    "prettier": "^2.5.1",
    "turbo": "^1.2.8"
  }
}
